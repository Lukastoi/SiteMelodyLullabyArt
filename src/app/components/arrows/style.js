import styled from "styled-components";

export const StyledArrow = styled.div`
    display:flex;
    margin:auto;
    cursor:default;
    img{
        width:64px;
    }
    :hover{
        cursor:pointer;
    }
`;

export default StyledArrow;